#include <stdio.h>
#include <stdlib.h>
#include "player.h"

void markBoard(double * b1, double * b2) { /* mark b1 with b2 */

}

int doMove(double * b1, double * raw_p, int diff) { /* do move based on environment */ 

	/* different moves for different difficulties */

	if(diff == 0) {
		// pick spot (NOT TRIED YET) with highest probability of being occupied based off raw
		;
	}
	else if(diff == 1) {
		// pick spot (NOT TRIED YET) with highest probability of being occupied based off raw AND b1
		
		;
	}
	else {
		;
	}
	
	int move = 0;
	return move;
}
